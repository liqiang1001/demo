import { ObjTransformPipe } from './obj-transform.pipe';

describe('ObjTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
